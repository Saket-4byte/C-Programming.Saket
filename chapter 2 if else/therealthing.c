#include <stdio.h>

int main(){
    int a;
    char ch ='a';
    if (3+2%5)
        printf("This works\n");
    if (a=10)
        printf("Even this works\n");
    if(-5)
        printf("surpringly even this works\n");
    if('a')
    printf("hello\n");
    if(ch)
    printf("hello\n");
    if (ch == 'g')
    printf("hello\n");
    if(ch='q')
    printf("blue\n");
    return 0;
}