#include <stdio.h>
#include<math.h> // it is a library functions
int main(){
    // int a;
    // printf("enter a number: ");
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("the square root is: %d",root);
    int q=pow(3,4);
    printf("%d",q);

    return 0;
}